#include <iostream>
#include "Alumno.hpp"

int main()
{
    // Declara aquí dos objetos de la clase Alumno, un con los datos default y otro con los datos que se muestran en el ejemplo.

    std::cout << "Datos de alumno por default\n";
    std::cout << "===========================\n";
    // Imprime los datos del objeto Alumno que se creó con los datos default.
    std::cout << "\n";

    std::cout << "Datos de alumno que no es por default\n";
    std::cout << "=====================================\n";
    // Imprime los datos del objeto Alumno que se creó con los datos que se muestran en el ejemplo.
    std::cout << "\n";

    std::cout << "Festejando !!\n";
    std::cout << "=============\n";
    // Genera el código necesario para que el alumno que se creo con los datos que se muestran en el ejemplo
    // cumpla un año más de vida.  Genera la salida que se muestra en el ejemplo.
}